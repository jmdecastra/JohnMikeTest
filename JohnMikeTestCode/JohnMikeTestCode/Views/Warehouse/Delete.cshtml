@model JohnMikeTestCode.Models.ELSTWAREHOUSE

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>ELSTWAREHOUSE</legend>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.ELSTWHID)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ELSTWHID)
    </div>

    <div class="display-label">
         @Html.DisplayNameFor(model => model.ELSTWHNAME)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.ELSTWHNAME)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
